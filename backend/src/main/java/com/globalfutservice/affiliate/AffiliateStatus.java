package com.globalfutservice.affiliate;

public enum AffiliateStatus {
    /** Applied, not yet approved. The code does not work until an admin approves it. */
    PENDING,
    ACTIVE,
    SUSPENDED
}
