{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",

  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://REPLACE-WITH-YOUR-API-HOST/api/:path*"
    },

    {
      "//": "The OAuth redirect legs, proxied for the same reason /api is.",
      "//2": "The refresh cookie is SameSite=Strict and set by the backend. Sent from",
      "//3": "the API's own domain it belongs to that domain, and the storefront on this",
      "//4": "one could never read it. Proxying keeps the whole sign-in first-party, so",
      "//5": "the cookie the provider flow issues is the same cookie the password flow",
      "//6": "issues. These two paths are also the reason the SPA catch-all below must",
      "//7": "not be the first rule that matches them.",
      "source": "/oauth2/:path*",
      "destination": "https://REPLACE-WITH-YOUR-API-HOST/oauth2/:path*"
    },
    {
      "source": "/login/oauth2/:path*",
      "destination": "https://REPLACE-WITH-YOUR-API-HOST/login/oauth2/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],

  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "source": "/index.html",
      "headers": [
        { "key": "Cache-Control", "value": "no-store, must-revalidate" }
      ]
    },
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self' https://checkout.razorpay.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: https:; font-src 'self' data: https://fonts.gstatic.com; connect-src 'self' https://api.razorpay.com; frame-src https://api.razorpay.com https://checkout.razorpay.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'"
        },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        {
          "key": "Permissions-Policy",
          "value": "camera=(), microphone=(), geolocation=(), interest-cohort=()"
        },
        {
          "key": "Strict-Transport-Security",
          "value": "max-age=63072000; includeSubDomains"
        }
      ]
    }
  ]
}
